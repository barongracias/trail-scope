# Comma-separated CORS allowlist for the frontend origin(s).
ALLOWED_ORIGINS=http://localhost:3000

# Optional path overrides (defaults shown).
# TRAILSCOPE_CHECKPOINT=checkpoints/model-best.pth
# TRAILSCOPE_UPLOAD_DIR=uploads
# TRAILSCOPE_RESULTS_DIR=results
