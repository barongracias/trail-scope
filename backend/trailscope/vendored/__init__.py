"""Vendored inference core copied from the thesis repo (see VENDOR_MANIFEST.md).

These modules are a frozen copy, not a dependency: there are no ``src.*`` imports
anywhere in this package (enforced by ``tests/test_no_src_imports.py``). Adapt only
as needed to stand alone; see each module's provenance header for its source.
"""
