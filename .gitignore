# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
frontend/out/

# Runtime artefacts
backend/uploads/
backend/results/
backend/outputs/
*.log

# Downloaded demo FITS (large; fetched on demand, never committed — ship only small
# cropped 8-bit PNGs derived from the public DECam frames).
.demo_cache/

# Env
.env
.env.local

# OS
.DS_Store

# NOTE: the locked checkpoint (model-best.pth, public) IS tracked deliberately so the
# image/demo is self-contained — do not add *.pth here.
# Demo data is public DECam + the narrow approved MeerLICHT exception only: the small thesis
# Fig. 5.4/5.5 display-PNG patches (frontend/public/demo/ml1_*.png) with acknowledgement.
# Do NOT add any other MeerLICHT data (raw/full-frame imagery, GT masks). See CLAUDE.md rule 5.

# Raw patch staging (process into frontend/public/demo, do not commit raw)
/demo_patches/
